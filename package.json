{
    "name": "autorank-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "node scripts/start.js",
        "start:backend": "cd backend && ..\\.venv\\Scripts\\python -m uvicorn server:app --reload --port 8001",
        "start:frontend": "cd frontend && npm start",
        "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}